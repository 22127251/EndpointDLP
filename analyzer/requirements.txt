# DLP Analyzer dependencies
# Python 3.13 / Windows 11 x64

google-re2       # RE2 regex engine (pre-built wheels available for Python 3.13 Windows)
pyyaml           # YAML policy loader

# Aho-Corasick (pyahocorasick requires compilation from source on Python 3.13 Windows)
# Before running `pip install pyahocorasick`, follow these steps:
#
#   1. Install Visual C++ Build Tools:
#      https://visualstudio.microsoft.com/visual-cpp-build-tools/
#      Select workload: "Desktop development with C++"
#      Minimum components: MSVC v143 compiler + Windows 11 SDK
#
#   2. Open "x64 Native Tools Command Prompt for VS 2022" from the Start menu.
#
#   3. Activate your venv:
#      .venv\Scripts\activate
#
#   4. pip install pyahocorasick
#      (pip will compile from source using MSVC automatically)
#
#   5. Verify: python -c "import ahocorasick; print('OK')"
#
pyahocorasick

# File extraction
python-calamine  # fast Rust xlsx/ods tabular extraction (prebuilt cp313 win_amd64 wheel); HARD dep, no fallback
lxml             # fast streaming parse of docx/odt tabular extraction (C libxml2)
openpyxl         # .xlsx plain-text path (tabular path uses python-calamine)
python-docx      # .docx plain-text fallback (tabular path uses lxml)
python-pptx      # .pptx (MS PowerPoint)
odfpy            # .odt / .ods / .odp plain-text fallback (tabular path uses calamine for ods, lxml for odt)
PyMuPDF          # .pdf  (pre-built wheels for Python 3.13 Windows via Stable ABI)
pymupdf-layout==1.27.2.2  # AI-guided find_tables(); version must match PyMuPDF==1.27.2.2
